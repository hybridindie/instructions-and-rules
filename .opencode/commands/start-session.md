---
description: Initialize an ACD implementation session — validate spec artifacts, assemble session context, prepare for one BDD scenario
---

Read and follow the complete command at `templates/_shared/commands/start-session.md`.
That file is the canonical source; this wrapper is a thin pointer. Substitute
placeholder tokens (`{{BACKEND_PATH}}`, `{{FRONTEND_PATH}}`, etc.) with the
values from this repo's configuration when interpreting the instructions.

$ARGUMENTS