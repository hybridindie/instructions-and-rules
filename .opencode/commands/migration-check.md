---
description: Validate a Supabase SQL migration against sql-standards before committing
---

Read and follow the complete command at `templates/_shared/commands/migration-check.md`.
That file is the canonical source; this wrapper is a thin pointer. Substitute
placeholder tokens (`{{DB_PROVIDER}}`, `{{BACKEND_PATH}}`, etc.) with the values
from this repo's configuration when interpreting the instructions.

$ARGUMENTS