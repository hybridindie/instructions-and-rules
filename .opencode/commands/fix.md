---
description: ACD pipeline-restore session — diagnose and fix a failing pipeline before any feature work resumes
---

Read and follow the complete command at `templates/_shared/commands/fix.md`.
That file is the canonical source; this wrapper is a thin pointer. Substitute
placeholder tokens (`{{BACKEND_PATH}}`, `{{FRONTEND_PATH}}`, etc.) with the
values from this repo's configuration when interpreting the instructions.

$ARGUMENTS