---
description: Run local CI checks (lint, format, types, constitution, tests) before pushing
---

Read and follow the complete command at `templates/_shared/commands/preflight.md`.
That file is the canonical source; this wrapper is a thin pointer. Substitute
placeholder tokens (`{{BACKEND_PATH}}`, `{{FRONTEND_PATH}}`, `{{TEST_BACKEND_CMD}}`,
`{{TEST_FRONTEND_CMD}}`, etc.) with the values from this repo's configuration
when interpreting the instructions.

$ARGUMENTS