---
description: "Frontend architect router for the genesis repo. Invoke for frontend design work — new components, pages, stores, service modules. Assumes the project's constitutional articles."
mode: subagent
permission:
  edit: deny
  bash: deny
---

Read and follow the complete agent definition at `templates/_shared/agents/frontend-architect.md`.
That file is the canonical source; this wrapper is a thin pointer. Substitute
`{{PROJECT_NAME}}` with "instructions-and-rules", `{{FRONTEND_PATH}}` with the
relevant frontend path, and `{{STATE_MANAGER}}` with "zustand" when interpreting
the instructions.